version: 2

sources:
  - name: stripe
    description: A clone of row data, related to payments form our app.
    database: raw
    schema: stripe
    tables:
      - name: payment
        description: Raw data for payments
        columns:
          - name: id 
            description: Primery key for stg_payments 
        loaded_at_field: _batched_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}
